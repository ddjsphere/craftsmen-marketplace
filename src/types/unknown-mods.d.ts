// Declare modules with versioned names (e.g. "@radix-ui/react-slider@1.2.3")
declare module '*@*';

// Fallback for any other unknown imports (keeps tsc from failing on Deno-style imports)
declare module '*';
